# Supabase (Project Settings → API Keys). Leave empty to use the local JSON store in /.data
SUPABASE_URL=
SUPABASE_SECRET_KEY=

# Passcode for the security desk. Required in production.
APP_PASSCODE=
